# Runtime dependencies for DocuWizard.
#   pip install -r requirements.txt

# --- web server (local only, binds to 127.0.0.1) ---
fastapi
uvicorn
python-multipart

# --- reading documents ---
python-docx        # .docx
openpyxl           # .xlsx / .xlsm
pdfplumber         # .pdf
python-pptx        # .pptx

# --- writing documents ---
reportlab          # PDF output
